<tr>
  <td><%= image_tag(gravatar_url, alt: user.name, class: "gravatar")%></td>
  <td><%= message.user.try(:login) %></td>
  <td><%= message.body %></td>

  <td>
  <button class="delete_msg" id="<%=message.id%>" href="<%=message_path(message.id)%>">Delete</button>
  </td>
</tr>
