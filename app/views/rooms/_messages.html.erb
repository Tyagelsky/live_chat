</tr>
<tr>
<td><%= gravatar_for current_user%></td>
<td><h5><%= message.user.name%></h5></td>
<p><%= message.body %></p>
<%if(message.user_id == current_user.id)%>
<div><%= link_to 'Delete', message, method: :delete, data: { confirm: 'Are you sure?'} %></div>
<%end%>
</hr>
</tr>
